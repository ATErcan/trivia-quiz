import { nanoid } from "nanoid";
import { useState } from "react";
import { QuizContainer } from "../styles/Quiz.styled";

const Quiz = ({ questions }) => {
  return <QuizContainer></QuizContainer>;
};

export default Quiz;
